import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'date-course-app-frontend',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
